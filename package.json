{
  "name": "twitch-npc-dialog-displayer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "build-early-prd": "vue-tsc -b && vite build --mode early-prd",
    "build-early-test": "vue-tsc -b && vite build --mode early-test",
    "build-pbtest": "vue-tsc -b && vite build --mode pbtest"
  },
  "dependencies": {
    "pug": "^3.0.3",
    "sass": "^1.89.2",
    "tmi.js": "^1.8.5",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.6",
    "@types/node": "^24.0.3",
    "@types/tmi.js": "^1.8.6",
    "@vitejs/plugin-vue": "^5.2.3",
    "@vue/tsconfig": "^0.7.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vue-tsc": "^2.2.8"
  }
}
